"""Напишете програма, която пресмята степента на дадено число без употребата на операцията степенуване.
Използвайте рекурсивна функция.
"""


def pow(n, k):
    if k == 1:
        return n
    return n*pow(n, k-1)


print(pow(5, 3))
